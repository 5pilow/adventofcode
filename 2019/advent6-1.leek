var rel = data.lines().map(d => d.split(')'))
var open = ['COM']
var i = 0
var d = 1
while (open.size()) {
	var nopen = []
	for o in open {
		nopen += rel.filter(r => r[0] == o).map(r => r[1])
	}
	open = nopen
	i += d * |open|
	d++
}
i
